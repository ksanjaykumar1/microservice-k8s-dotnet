apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mssql-claim-new
spec:
  resources:
    requests:
      storage: 2Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  storageClassName: standard  # Make sure this exists

